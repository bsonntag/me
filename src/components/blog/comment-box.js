import Remarkbox from 'react-remarkbox';
import styled from 'styled-components';

const CommentBox = styled(Remarkbox)`
  margin-bottom: -2px;
  min-height: 184px;
  min-width: 100%;
  width: 1px;
`;

export default CommentBox;
